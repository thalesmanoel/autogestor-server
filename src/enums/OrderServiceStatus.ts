enum OrderServiceStatus {
  OPENED = "opened",
  PENDING = "pending",
  BUDGET = "budget",
  IN_PROGRESS = "in_progress",
  COMPLETED = "completed",
  CANCELED = "canceled"
}

export default OrderServiceStatus;
